[uwsgi]
chdir = /home/runner/TourismExplorex
module = app:app  # Adjust to match your app's entry point
callable = app  # Flask/Django callable
master = true
processes = 2
http-socket = 0.0.0.0:%(env:PORT)
vacuum = true
die-on-term = true
